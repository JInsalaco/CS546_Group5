<nav class="nav-side nav-container__right flex-c">
  <h2 class="info__title">Account</h2>
  <el-avatar class="info__avatar" :size="100" :src="userInfo?.profilePic ?? '/public/img/default.png'" alt="avatar">
  </el-avatar>
  <div class="info__username">
    \{{displayName}}
  </div>
  <ul class="info__contact-container">
    <li>
      <i class="iconfont icon-phone"></i>
      \{{userInfo?.phoneNumber ?? '(xxx)xxx-xxxx'}}
    </li>
    <li>
      <i class="iconfont icon-email"></i>
      \{{userInfo?.email ?? 'example@stevens.edu'}}
    </li>
  </ul>

  <div class='info__friends-list-container'>
    <el-button class="btn-collapse" type="text" :data-show-list="showFriendsList"
               @click="showFriendsList = !showFriendsList">
      Friends
      <i class="iconfont icon-down"></i>
    </el-button>
    <el-collapse-transition v-if="myFriendsList.length">
      <ul class="info__friends-list" v-show="showFriendsList">
        <li v-for="item in myFriendsList" :key="item" class="aside-v flex">
          <el-avatar class="aside-h" :src="item.url" alt="avatar"></el-avatar>
          <span class="aside-h">\{{item.name}}</span>
        </li>
      </ul>
    </el-collapse-transition>
    <el-empty v-else description="No Data"></el-empty>
  </div>
</nav>